name: sms_service
description: "send sms for almost free"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
 
version: 1.0.0+1

environment:
  sdk: ^3.5.4
 
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2
  hive_flutter:
  firebase_core: ^2.15.1
  firebase_messaging: ^14.6.7
 
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

 
  flutter_lints: ^4.0.0

 
flutter:
 
  uses-material-design: true
 